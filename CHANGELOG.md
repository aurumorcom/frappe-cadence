## Features

* **Webhook Secret Support for Listmonk Client**
  Added webhook secret configuration, management, and secret retrieval logic to the Listmonk client, complete with integration tests (`da2fa37`, `dae7aaf`, `571bba5`).
